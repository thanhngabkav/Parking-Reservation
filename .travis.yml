language: java
jdk: oraclejdk8
before_install:
  - chmod +x mvnw
install: true
script:
  - ./parking_reservation_1.0/mvnw clean install
  - bash <(curl -s https://codecov.io/bash)
 cache:
    directories:
    - $HOME/.m2
